# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Author {
  firstName: String
  id: Int!
  lastName: String
  posts: [Post!]!
}

type Comment {
  author: String!
  id: Int!
  postId: Int!
  text: String!
}

input CommentInput {
  author: String!
  text: String!
}

type Mutation {
  addComment(comment: CommentInput!, postId: Int!): Comment!
  upvotePost(input: UpvotePostInput!): Post!
}

type Post {
  author: Author!
  content: String
  id: Int!
  title: String!
  votes: Int
}

type Query {
  findAllAuthor: [Author!]!
  findOneAuthor(id: Int!): Author
  post(id: Int!): Post
  posts: [Post!]!
  searchAuthors(firstName: String, lastName: String! = "", limit: Int! = 10, offset: Int! = 0): [Author!]!
}

type Subscription {
  commentAdded: Comment!
}

input UpvotePostInput {
  postId: Int!
}