// Production environment configuration
// Uses relative API URL for production deployment
export const environment = {
  production: true,
  apiUrl: '/api/v1' // Production API URL (relative path)
};
